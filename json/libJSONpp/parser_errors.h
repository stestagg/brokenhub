#pragma once

#include "types.h"

namespace JSON{

	namespace errors{
			/*! \brief an array of error descriptions JSON::errors::errors[10] is the description for error 10. */
		const extern achar *errors[];
	}	
}

